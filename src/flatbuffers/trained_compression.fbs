include "kmeans.fbs";
include "huffman_decoder.fbs";

namespace memb.wire;

table TrainedQuantizedNode {
    word: string(key);
    compressed_values: [uint8];
}

table Trained {
    nodes: [TrainedQuantizedNode];
    decoder: HuffmanDecoder;
    clusterizer: KMeansClusterizer;
}
